“Here’s a Ringer brief Read it, pick the closest skeleton from ‎`templates/`, and write me a ‎`swarm.json` manifest for it.”

--- BRIEF ---
THE JOB: Turn every SCLA lesson script into a polished, engaging,
fully-illustrated branded MP4 and publish it live on Wistia. Proof is that the
video is up and plays. "Polished" means it looks like a whole team made
intentional decisions on every element. "Engaging" means moving illustrations
that explain the idea, frequent scene changes, no dead air. Scope is everything
not yet on Wistia, verified against Wistia itself. Build the production path
fresh on HyperFrames. Prove it on two videos, then run one program at a time,
publishing unattended while I spot-check after.

WHAT EXISTS:
- Scripts: projects/video-production/lesson-scripts/<program>/refined/ — ~36
  across four programs, plus 2 unrefined at program root.
- Brand and video essentials — colors, type, spacing floors, components, motion
  contract, narration voice IDs, program display names:
  projects/video-production/design-system/config/tokens.yml
- Brand SVGs and fonts: projects/video-production/design-system/assets/
- Wistia projects (one per program), title convention, and the upload-token
  pointer: config/endpoints.json, "wistia" section. The token cannot delete.
- Raw scripts are not on disk — they live on the SCLA dashboard, re-scrapable.
- published.tsv is stale. Not evidence.
- projects/video-production/_archive/ is OFF LIMITS. Do not read, cite, port, or
  restore it.

OPEN QUESTIONS THE TEAM SHOULD ANSWER:
- Do the refined scripts read as narration? Fix missing connector words and
  anything that makes the voice stumble. Fix in place.
- Any words the voice will mispronounce? Fix in the script before synthesis.
- Is projects/video-production/ organized for this process, and does the repo's
  routing point at the right things? Bring both into alignment.

DELIVERABLES:
- A verified live-on-Wistia gap list.
- 2 pilot videos shown to me before anything scales.
- Every lesson video built, rendered, and published to its program's Wistia
  project, in order: early-career-boost, mid-career-momentum, career-transitions,
  entrepreneur-accelerator. Finish a program before starting the next.
- Per video: title per the convention in the registry; description derived from
  that lesson's own opening narration; thumbnail = the opening slide pulled from
  the finished MP4 after entrances settle, not frame 0.
- HyperFrames workspace files preserved on disk per lesson, so a correction is an
  edit, not a rebuild.
- A written step-by-step process guide: each stage, and what must be true to
  leave it.
- A live status record for every lesson, readable from disk alone, showing its
  stage and next action — so a dead session resumes without guesswork.
- Executable checks for every constraint below, written fresh. A preference that
  isn't a check doesn't count.
- Corrected refined scripts.

TASTE AND CONSTRAINTS:
- Fully illustrated. No avatars.
- No timing drift, no static stretches, no text/illustration overlap, no sloppy
  spacing, no sameness between scenes or between videos.
- No audio gaps, no long pauses, no mispronunciations.
- One palette for all four programs. Program identity lives only in the title
  card eyebrow; distinctiveness comes from illustration, and must be checked.
- Everything visual comes from tokens.yml. Nothing hand-picked.
- Publishing is unattended after the pilots. The upload token cannot delete —
  get it right before it goes up, and confirm on video one that the credential
  can set a description and a custom still.

TIMEBOX: All four programs live on Wistia within 3 days of 2026-08-02. Three pilots
first, then run flat out.
--- END BRIEF ---