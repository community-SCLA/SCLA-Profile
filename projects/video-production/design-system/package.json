{
  "name": "design-system",
  "private": true,
  "type": "module",
  "comment": "Token/asset store since 2026-08-05 (template lane retired) — no renderable compositions live here. This file's remaining job is the hyperframes version pin below: check_layout.py reads it and every render runs at it. Bump deliberately, never drop.",
  "scripts": {
    "pin": "npx --yes hyperframes@0.7.79 --version"
  }
}
