# Community email scope

Preserve all existing SCLA dashboard templates and the separate Notion database experiment.
Keep credentials in Infisical. Never commit actual Notion snapshots, draft content, generated emails, or private page identifiers.
Use `bash scripts/lint-refs.sh` for verification. The project test command is `bash projects/community-email/run.sh test`.
Read `docs/setup.md` for the connection and content contract. There is no email-sending feature.
