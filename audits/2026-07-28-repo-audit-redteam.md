# Red-Team Report — repo-audit-brief-2026-07-28.md

**VERDICT: 🔴 — 5 blockers, 12 repairs, 3 notes. The brief's single most consequential "verified" claim (R8's byte-identical duplicates) is false, and executing it as approved would break the video pipeline.**

**RUBRIC REPAIRED: the standard file itself needed three corrections (STD-8 reclassified, STD-15 gloss marked as inference, STD-31 added for an on-point AGENTS.md doc passage the fetch missed). All 30 original quotes were re-fetched live and are verbatim — the rubric's substance held; its labels needed surgery.**

All 🔴 and 🟡 findings below have been applied to the brief in place (see its Revision log). Six agents ran independently: (1) Fact Checker, (2) Citation Checker, (3) Risk Re-classifier, (4) Confused Reader, (5) Blind Spot Hunter, (6) After-Picture Auditor. Where two agents' outputs conflicted (the linter's exit code), the orchestrator resolved it from source: `scripts/lint-refs.sh:124-127` exits 1 on any warning — the brief's "fails with 8 warnings" was **correct**, and Agent 1's contrary observation was discarded.

---

## Findings

| ID | Sev | Agent | Brief § | What's wrong | Evidence | So what | Now what |
|---|---|---|---|---|---|---|---|
| RT-1 | 🔴 | 1, 3, 5 | R8, §1, §4 | "Ten identical skill files in two places… byte-identical (verified)" is false. The 10 overlapping `.claude/skills/` entries are **git symlinks** (mode `120000`) into `.agents/skills/` — the same files, not copies. `.agents/` is the *primary* store (22 skills; `.claude/skills/` = 6 real + 10 links). | `git ls-files -s .claude/skills` → 10 entries mode 120000; `readlink -f .claude/skills/hyperframes` → `.agents/skills/hyperframes`; `render-qa/src/synth_narration.py:86-87` hardcodes `.agents/skills/hyperframes-media/scripts/heygen-tts.mjs`; `design-system/frame.md:524` routes builders there; `decisions/log.md` 2026-07-13: "the files remain in `.agents/skills/` for reference" | Executing R8 as confirmed would dangle 10 symlinks, kill 10 of 16 skills (the whole hyperframes stack) and the TTS step of every build — the video factory dies after an "approved" change | R8 rewritten: do NOT archive; re-plan (materialize links first, or bless `.agents/` as canonical); prior confirmation void — it was given against a false premise |
| RT-2 | 🔴 | 3, 1 | S1 | S1 mislabeled SAFE. (a) The hooks were once wired (commit `bc1dbde`) and **deliberately removed** (`32ce4d0`) — re-arming reverses a recorded decision; "never plugged in" is false as history. (b) `pre-tool.sh` hard-blocks at 500 tool calls; its 80-call version "killed video sessions mid-build" (`decisions/log.md:172`); `doctor.sh` still defaults to 80. (c) `hooks/governance-check.sh` `APPROVED_ROOT` (lines 76-82) approves the deleted `_archive` and omits `.github`/`.vscode`/`.agents`/`skills-lock.json` — armed, it blocks S12's own workflow file; the operative list is **hardcoded in the script**, which S2's "words-only" edit never touches. (d) "Tested" unsupported: `~/.claude/logs/` doesn't exist; these hooks never ran here. (e) The "7" include `doctor.sh` (a manual CLI that exits 1 as a hook) and `cleanup-worktrees.sh` (`git worktree remove --force` on every Stop). | Commit history of `.claude/settings.json`; `decisions/log.md:172`; `hooks/governance-check.sh:76-82` | Arming untested, once-rejected enforcement without review could block a render mid-SHIP and veto later safe steps — the exact class of silent breakage the brief exists to prevent | Moved to §6 as **R10** with per-hook review requirements |
| RT-3 | 🔴 | 3 | S6 | "Plugged into nothing (verified)" is false. `scripts/lint-refs.sh:69` lists `hooks/skill-rules.json` as a critical file and `:115` parses it; `scripts/merge-settings.py:57` registers `skill-eval.sh`; `setup.sh` copies `hooks/*` on rebuild. | Read from source this session | Archiving as-is makes the linter warn and exit 1 **forever** — and with S12 live, CI goes permanently red | Moved to §6 as **R11**: retire only with coordinated edits to lint-refs.sh + merge-settings.py |
| RT-4 | 🔴 | 3, 1 | S12, §7 | S12 is scheduled pre-gate (Step 3), but lint-refs exits 1 today with 8 warnings that are **all `_archive/` references only R3 (post-gate) can clear**. As ordered, it ships a check that is red on every push from day one — manufacturing the exact "silently failing automation" pattern the brief condemns. Citation is also a stretch (STD-8 is about Claude Code hooks, not CI). | `bash scripts/lint-refs.sh` behavior read from source: warnings→exit 1; all 8 warnings are `_archive/` refs in CLAUDE.md/MAP.md/GOVERNANCE.md | A permanently red check trains everyone to ignore the alarm — worse than no alarm | S12 re-sequenced after R3; citation restated as general hygiene (analogy to STD-8's principle) |
| RT-5 | 🔴 | 5, 3 | missing; S3/S13 | The brief's danger inventory misses the live one: `/home/codespace/.claude` is a symlink into a **second clone of this same repo** (the Codespaces dotfiles repo), whose working tree contains **unignored live OAuth credentials** (`.claude/.credentials.json`) plus session transcripts/history — while tracked settings pre-approve `Bash(git *)` and tracked `.claude/settings.local.json` pre-approves `Bash(git push *)` and sets `enableAllProjectMcpServers: true`. One `git add -A && git push` publishes a live credential. S13's "worst case is an extra confirmation prompt" is false. | `readlink /home/codespace/.claude` → `/workspaces/.codespaces/.persistedshare/dotfiles/.claude`; `git remote -v` there → same origin, HEAD `469f686`; `git status` shows untracked `.claude/.credentials.json`; `.gitignore` has no rule for it; `git ls-files .claude` includes `settings.local.json` | Credential exposure is one pre-approved command away, today, independent of the brief's whole plan | New **S14** (ignore rules — do first, ~10 min) and **R12** (dotfiles architecture, un-track settings.local.json, drop push pre-approval) added to the brief |
| RT-6 | 🟡 | 5, 3 | S3, S13 | The "machine-wide settings file" model is wrong: it's the tracked `.claude/settings.json` of that second clone. S3's edit is an uncommitted local change a dotfiles re-sync reverts; "leaks into every other project" is currently moot (no other projects) — but every future Codespace for any repo inherits this KB + the `rm` pre-approval. Dormant fault line: `.devcontainer/postCreate.sh` → `setup.sh` → `merge-settings.py` would re-add the old hook stack if its interactive prompt ever stops crashing it. | Agent 5 F2; Agent 3 S3 analysis | The double-fire fix as written quietly un-fixes itself on rebuild | S3/S13 rewritten with the real mechanism; durable fix routed to R12 |
| RT-7 | 🟡 | 6, 3 | S4, R3, §4, §7 | The same `_archive/` edit is classified twice: SAFE in S4 (Step 1, pre-gate) and RISKY in R3 (gated, "rewriting the rules ratifies a mistake"). Executing S4 front-runs the R3 decision. MAP.md's AFTER line is also missing its R3 ⚠️. | R3's text names MAP.md; S4's text includes "routes to the deleted `_archive/`" | The plan's own risk gate is bypassed by its own Step 1 | `_archive/`-routing lines carved out of S4 into R3; AFTER tree annotated |
| RT-8 | 🟡 | 1, 6 | §1, §4, R7 | Count errors: root is **30** items pre-audit, not 31 (BEFORE tree also omits `.devcontainer/`, `.vscode/`, `.mcp.json`, `.gitignore`); `scripts/` is **12** items, not 13; render-qa "12 code files" not reproducible (10 top-level `.py`, 13 with `tests/`); hooks/ is 9 scripts + 1 JSON, not "10 scripts"; the scheduled run fires **roughly hourly**, not "every two hours"; "brand/ lacks its promised guide file" misleads — `brand/brand-guide.md` exists; what's missing is a README hub; the "31 → ~27" summary arithmetic is unreachable (honest number: 32 today → 29). | `ls -A` counts; commit timestamps 00:56/01:57/02:56…; Agent 6 arithmetic walk | Wrong counts in a document whose whole pitch is "every fact checked" corrode trust in the right ones | All counts corrected in the brief |
| RT-9 | 🟡 | 1 | §8 Q5 | `.s3_setup_FVC8bRn6m` is NOT "owned by the system's root user and locked (readable metadata only)" — it's owned by `codespace`, mode 600, and its content is readable (a dpkg package listing). | `stat`: owner codespace; content read this session | The stated reason for leaving it in place is false; it can simply be deleted or knowingly kept | Q5 rewritten with the true facts |
| RT-10 | 🟡 | 2, 3 | R4 | R4 cites STD-13 for "one discovered instruction file per folder scope" — STD-13 says nothing of the sort. The docs have a directly on-point passage (now STD-31) whose endorsed pattern is the **reverse** of R4's plan (keep AGENTS.md, have CLAUDE.md import it). Repo evidence also contradicts the recorded "nothing else reads AGENTS.md": `.agents/skills/hyperframes-core/references/subagent-dispatch.md:30` (Codex standing grant lives in the workspace AGENTS.md) and `render-qa/BUILD-LOG.md:552`. | Live fetch of code.claude.com/docs/en/memory; repo grep | R4 could sever a non-Claude tool's entry point despite its "confirmed" status | R4 re-cited to STD-31, alternative pattern stated, confirmation flagged as contradicted |
| RT-11 | 🟡 | 2 | §3, rubric | STD-8 was classified flat HARD RULE (its second half is practice advice); STD-15's label smuggled the inference "no registry" into a file claiming "none of it is my opinion". | Live page fetches; the standard's own STD-25 shows the correct split pattern | A guideline hardened into a hard rule is exactly the rubric failure this review exists to catch | Standard file corrected; brief's §3 synced |
| RT-12 | 🟡 | 2 | §2, §5 | STD-22/23 are scoped to CLAUDE.md but applied to GOVERNANCE.md/MAP.md/endpoints.md without a note; §2 row 5's "no hand-maintained list is needed" is inference presented as doc fact; §2 row 7's "nothing (hooks)" drops STD-28's caveat — which matters here, since this repo's reminder hooks emit output (the non-zero-cost case). | Agent 2 C-5 + §2 checks | Small distortions in the teaching section propagate into future decisions | One-line scope note + rephrasings applied |
| RT-13 | 🟡 | 6 | §4 | AFTER-tree defects: phantom effects (skills-lock "on the books (S2)" — S2 never named it; `.s3` cited to S7, which never mentions it; repo-standard "kept" by no item), **today's brief vanishes** from the AFTER picture entirely, invisible items (R2, R9, S3, S10's hubs), S6's retirement drawn on the wrong folder (`.claude/skills/` instead of `hooks/`), and the summary row "all real entry points" is false while the junk `.s3` file stays. | Agent 6 line-by-line walk | The after-picture is the owner's approval basis; phantom improvements get approved but never happen | Trees redrawn; every item now visible, every conditional line marked |
| RT-14 | 🟡 | 4, 5 | S6/S7/R8/R9, §9 | The archive-destination paradox: every "archive" action targets a folder the brief itself says was deleted, and no row names a real destination. Meanwhile nested archives are alive: `projects/video-production/_archive/status.md` is tracked (and stale), and `renders-hyperframes/_archive/` is the hook-mandated destination — so R3's rewrite can't treat the archive rule as purely dead. | Agent 4 Set 2; Agent 5 F9 | A reader cannot execute "archive X" when the brief says the archive doesn't exist | Destination defined (recreate root `_archive/`, rule scoped in R3); nested archives folded into R3's scope |
| RT-15 | 🟡 | 4, 6 | §6, §7, §8, §1 | Sign-off logic contradictions: the §6 note says four items are pre-confirmed but six rows carry "confirmed"; §7's gate demands answers to all of §6 including them; R2 is "✅ do it" while its enabling question (Q1: where does the schedule even live?) is open and the dependency unflagged; S10 hides an unresolved either/or inside the "no review needed" list; "15 minutes of yes/no answers" misdescribes §8's investigative asks. | Agent 4 Set 6; Agent 6 §7 check | The owner cannot tell which answers they actually owe — the gate becomes theater | Confirmation note corrected (six, all treated as confirm-or-veto at the gate), R2↔Q1 flagged, S10 either/or resolved (soften now, hubs optional), §1 estimate honest |
| RT-16 | 🟡 | 4, 1 | §1, §9, R6, §4 | Reader failures: glossary is last and never pointed to; ~15 body terms missing from it; R6 never names the second program (it's `career-transitions/`); the never-answered question "did the AI already delete anything with that pre-approval?"; ⚠️ means two different things in one legend; 2 of the 10 hooks/ items unaccounted for in S1+S6 arithmetic; the "215-line file" never identified (it's `projects/video-production/avatar-pipeline/CLAUDE.md`). | Agent 4 Sets 1-6; Agent 1 items 12, 32 | The audience is explicitly non-technical; each gap is a place they stall or panic | Glossary pointer added up top, key terms added, program named, reassurance line added (the only wholesale deletion found in history is `_archive/`, handled in R3), legend split, arithmetic reconciled |
| RT-17 | 🟡 | 5, 3 | §5 (new), various | Real findings the brief missed: dead `.gitignore` negation (`!.vscode/tasks.json` can't work under `.vscode/` — the file survives only because it was force-added); `.devcontainer/postCreate.sh` is dead code — and it's the only home of the post-incident brand anti-fabrication guard and the hooks installer chain; both reminder hooks fire on bare substring matches (one fired on a read-only grep this session, asserting "a Wistia upload just ran" and urging the purge script); half-megabyte tracked append-only logs (`snag-log.md` 502 KB); permission list mixes two syntaxes and lists `cp` twice; `endpoints.md` publishes Infisical clientId/identityId; `new-from-template`'s body calls nonexistent `scripts/ingest.py` (so S9 alone advertises a broken skill); S11's "leaf files nothing references" is false (`decisions/log.md:124,311` cite two audits filenames); R5 actually has a third candidate (`preview.sh`, which snag-log tells humans to run); R7's scripts compute paths positionally (`parents[3]`) and the build window is open now (renders-hyperframes/ is empty); brief-27 is untracked, so "every move can be undone" fails for it until committed. | Agent 5 F4/F5/F7/F8/F12/F14 + Agent 3 notes, each with command output in their reports | Each is either a latent breakage or a false safety claim | New S15/S16 added; S7/S9/S11/R5/R7 amended; postCreate/logs/Infisical routed to §8/R12 as owner calls |

### 🟢 Notes (no correctness impact)

| ID | Agent | Note |
|---|---|---|
| RT-N1 | 2 | The rubric survived a full adversarial re-fetch: 30/30 quotes verbatim, every attribution correct, and the brief's §3 transcription was byte-identical to the standard file. Zero drift. |
| RT-N2 | 1 | The brief's §2 row 2 is exemplary standards use: it preserved "a target, not a law" and correctly took no action on the 215-line file. |
| RT-N3 | 3 | The brief's ⏸️-wait instincts (R5, R7, R9) were all vindicated by deeper evidence the brief didn't have. |

---

## What the brief got right

Six adversarial agents could not break the brief's core diagnosis, and that is real signal:

- **The two failing automations are exactly as described.** 100 of the last 100 drive-sync runs failed (`gh run list` reproduced it), the failure cause is precisely the unset `GDRIVE_TARGET` variable, and the scheduled video run's 24 firings against the same TTS wall are all in commit history. Both "verified" labels held.
- **The paperwork-vs-reality diagnosis held everywhere it was checked.** MAP.md's ≥7 false claims were re-enumerated and confirmed one by one; GOVERNANCE.md's 7 claimed "live" protections against an empty deny-list confirmed; `rm`/`cat`/`head`/`tail`/`find` pre-approvals confirmed in both settings files; the `_archive/` deletion in commit `bed5ff7` with no decision-log entry confirmed; "nine templates" stale in exactly 4 files against a real count of 12, confirmed file by file.
- **The contradictory "Rebuild" lesson is real and quoted accurately** — the two refined scripts define it incompatibly, word for word as the brief said.
- **The rubric was fetched honestly.** Every quote verbatim, guideline/hard-rule discipline mostly right (one split missed), and the three stated gaps in the official docs are genuinely gaps.
- **Structure and instincts.** The SAFE/RISKY split as a concept, the sign-off gate, the what/so-what/now-what framing, and the wait-states on R5/R7/R9 all survived. What failed was narrower and repeatable: claims of "verified" for things checked with the wrong tool (`cmp` through symlinks), and SAFE labels assigned without reading the scripts the items would activate.
