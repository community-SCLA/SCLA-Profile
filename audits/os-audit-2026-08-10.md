# OS Audit — 2026-08-10

**Scope:** focused disk-space rescan; still-valid non-disk evidence is carried
forward from the 2026-08-08 audit.

**Knowledge current through: 2026-08-10 18:27 UTC for video production.**

| Check | Verdict | Worst finding |
|---|---|---|
| Routing integrity | RED | The routed test path `projects/video-production/render-qa/tests/test_variety.py` is still missing. |
| Index truth | YELLOW | Live state must come from `run.sh status --json`; recovery snapshots remain non-authoritative. |
| Freshness | RED | The gate rejected one approved lesson at 3,104 MB free; a concurrent cache clear then raised free space to 4,231 MB, only 135 MB above the gate. |
| Bloat/duplication | RED | Repeated npm execution caches have regrown to 4.3 GiB; three currently inactive trees consume about 2.1 GiB. |
| Hygiene | YELLOW | Cache cleanup is manual, so the same disk-pressure failure has recurred within two days. |
| Context placement | RED | Recovery checkpoints look disposable by size but are required for safe resume and must not be treated as cache. |

## Failure-mode exposure

| Mode | Exposure | Driven by |
|---|---|---|
| Poisoning (false) | HIGH | The routed missing test and stale recovery artifacts from the prior audit. |
| Bloat (too much) | HIGH | Three inactive HyperFrames execution trees consume about 2.1 GiB, and cache growth has already stopped shipping once. |
| Confusion (wrong or missing) | HIGH | Active recovery checkpoints and narration can be mistaken for disposable build output. |
| Clash (contradictory) | HIGH | Ad-hoc workspace deletion would conflict with the resume and approval contracts. |

## Since last audit

- Free space fell from about 6.3 GiB after the 2026-08-08 cleanup to 3.1 GiB
  (90% used). During this read-only audit, a concurrent action cleared npm's
  1.2 GiB download cache and raised free space to 4,231 MB (87% used). This is
  only 135 MB above the pipeline gate. [bloat]
- The npm execution cache regrew to 4.3 GiB. Three currently inactive,
  unreferenced HyperFrames trees account for about 2.1 GiB. [bloat]
- Published lessons increased from 16 to 22, while in-flight workspaces fell
  from 23 to 17. [no failure]
- The pipeline reported five exact cuts approved and ready to ship, and
  `m2_four-kinds-of-career-transition_career-transitions` was rejected at the
  4,096 MB free-space gate. After the concurrent cache clear, live status no
  longer reports an active low-disk condition. [confusion]
- The branch is now ten commits ahead of its remote and also has uncommitted
  lesson work, so local workspaces and Git recovery data are not disposable.
  [confusion] [clash]

## What would make your agent wrong-answer you today

- Calling all of `source-revisions/` cache would put about 2.49 GiB of exact
  rollback checkpoints at risk; active resumes depend on them. [confusion]
- Deleting an npm execution tree currently used by a running HyperFrames
  process would interrupt review or inspection. [confusion]
- Treating Wistia publication as permission to delete local source would remove
  the only editable copy of untracked lesson compositions. [confusion]

## Findings by check

### Routing integrity

- The missing `test_variety.py` route from the prior audit is unchanged.
  [poisoning]

### Index truth

- `bash projects/video-production/run.sh status --json` reports 22 published,
  17 in flight, five approved cuts ready to ship, one low-disk rejection, and
  zero orphan workspaces. This live control output, not the open human-facing
  status file, is authoritative. [confusion]

### Freshness

| Feed | Raw date | Ingested date | Cadence | Verdict | What's missing |
|---|---:|---:|---|---|---|
| Live video workspace evidence | 2026-08-10 18:25 | 2026-08-10 18:25 | Event-driven | FRESH | Nothing material |
| Wistia publication ledger | 2026-08-10 | 2026-08-10 | Per publish | FRESH | Nothing material |
| Git remote backup | 2026-08-10 local | Remote ten commits behind, plus uncommitted work | On demand | DRIFTING | Current recovery state |

### Bloat and duplication

| Candidate | Size | Safety | Basis |
|---|---:|---|---|
| Three inactive npm HyperFrames trees (`2d24f54ded6cd194`, `b67ddc87314872f7`, `c7c4026b235a35e6`) | 2.1 GiB | Safe after one final process check | No running process or repository reference uses them; `npx` can recreate them. |
| `/home/codespace/.npm/_cacache/` | 580 KiB now | Already cleared concurrently | It measured 1.2 GiB earlier in this audit, then an external action cleared it. |
| Workspace `.thumbnails/`, `.waveform-cache/`, and `node_modules/` | about 55 MiB | Safe but low value | Regenerable; deleting them adds review/render setup time. |
| HyperFrames browser, speech, and transcription caches | about 1.2 GiB | Last resort | Regenerable, but re-downloading models and browsers slows production. |
| Playwright browser cache | 656 MiB | Last resort | Regenerable, but current visual tooling depends on it. |
| Five local MP4s already recorded in Wistia | 78 MiB | Defer | Small saving; the local files support verification and archive guards. |
| `source-revisions/` | 2.49 GiB | Do not delete | Immutable, exact resume checkpoints. |
| Published workspace `source-revisions/` subset | 829 MiB | Do not delete ad hoc | Pipeline cleanup does not classify them as cache; an explicit retention policy is required. |
| `.git/` | 798 MiB | Do not prune now | Branch is ahead and working tree has uncommitted lesson work. |

- The remaining immediate safe cache batch is about 2.1 GiB and should raise
  available space from 4,231 MB to roughly 6.3 GiB. [bloat]
- Active npm trees `702923228c2ce1e6` (HyperFrames 0.7.105),
  `8238c6166ab8a4c1` (HyperFrames 0.7.79), and
  `9439993576a80c16` (HyperFrames 0.7.79) had live processes during the audit
  and must be preserved. The 0.7.105 path is also hardcoded in a tracked
  experiment. [confusion]

### Hygiene

- Disk-pressure cleanup remains manual; the same cache class that was cleared
  on 2026-08-08 has already regrown enough to stop shipping. [bloat]
- No files were removed in this audit. [no failure]

### Context placement

- Video safety rules require usable narration, snapshots, and source
  checkpoints to survive resume. Their size does not make them caches.
  [confusion] [clash]
- Published editable sources are local-only. Wistia is durable for the rendered
  MP4, not for the HTML composition and narration source. [confusion]

## Questions for the owner

- Approve the approximately 2.1 GiB remaining immediate cache cleanup, or keep this audit
  read-only?
- After the current batch ships, should editable published sources move to
  durable external storage so old checkpoints can be pruned through a new,
  pipeline-aware retention policy?

## Fix list (batched, await approval)

- **Batch A — stabilize shipping:** remove only the three inactive npm execution
  trees; then verify free space and live status. The download cache has already
  been cleared by a concurrent action.
- **Batch B — resume safely:** retry the one low-disk rejection through
  `run.sh retry` only after the space check passes; leave all workspaces intact.
- **Batch C — prevent recurrence:** pin or reuse one HyperFrames execution tree
  and add a cache-size warning before the filesystem falls below the 4 GiB gate.
- **Batch D — long-term retention:** back up published editable sources, define
  checkpoint retention, and implement pruning through pipeline-aware tooling.
